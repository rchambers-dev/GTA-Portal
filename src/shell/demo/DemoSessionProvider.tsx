"use client";

/**
 * Back-compat shim — prefer PortalSessionProvider / usePortalSession.
 */
export {
  PortalSessionProvider,
  DemoSessionProvider,
  usePortalSession,
  useDemoSession,
} from "./PortalSessionProvider";
