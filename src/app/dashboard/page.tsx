import { FeatureStubScreen } from "@/features/learner-lifecycle";

export default function DashboardPage() {
  return (
    <FeatureStubScreen
      title="Overview"
      description="Summary metrics on the Learner Lifecycle board are the Phase 1 operational focus. A dedicated overview dashboard can aggregate the same ports later."
    />
  );
}
